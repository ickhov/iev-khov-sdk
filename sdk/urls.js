const baseURL = 'https://the-one-api.dev/v2';

const urls = {
  book: `${baseURL}/book`,
  movie: `${baseURL}/movie`,
  character: `${baseURL}/character`,
  quote: `${baseURL}/quote`,
  chapter: `${baseURL}/chapter`,
};

export default urls;